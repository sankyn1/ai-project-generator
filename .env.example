# Server Configuration
PORT=5000
NODE_ENV=development

# Note: API keys are now configured through the web interface
# and stored in browser localStorage for security and flexibility

# Optional: Default API keys (not recommended for production)
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# Figma API (Optional)
FIGMA_ACCESS_TOKEN=your_figma_token_here

# Database (Optional - for storing projects)
DATABASE_URL=postgresql://username:password@localhost:5432/ai_project_generator